machine:
  java:
    version: openjdk8

dependencies:
  pre:
    - ./gradlew install

test:
  override:
    - ./gradlew check -Dscan
    - ./gradlew -p acceptance-test test -Dscan

#deployment:
#  - test ${TRAVIS_TAG} && ./gradlew bintrayUpload

